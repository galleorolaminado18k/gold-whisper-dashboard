{
  "rewrites": [
    { "source": "/(.*)", "destination": "/dist/index.html" }
  ],
  "build": {
    // Eliminar la propiedad 'command' ya que no es válida
    // "command": "npm run build",
    "output": "dist"
  }
}